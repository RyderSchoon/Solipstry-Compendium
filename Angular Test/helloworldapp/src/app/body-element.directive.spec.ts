import { BodyElementDirective } from './body-element.directive';

describe('BodyElementDirective', () => {
  it('should create an instance', () => {
    const directive = new BodyElementDirective();
    expect(directive).toBeTruthy();
  });
});
